import { ChainAdjustmentPipe } from './chain-adjustment.pipe';

describe('ChainAdjustmentPipe', () => {
  it('create an instance', () => {
    const pipe = new ChainAdjustmentPipe();
    expect(pipe).toBeTruthy();
  });
});
